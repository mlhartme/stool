apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mhm-https
spec:
  host: mhm.cp.pearl.server.lan
  port:
    targetPort: https
  tls:
    termination: passthrough
  to:
    kind: Service
    name: mhm
    weight: 100
  wildcardPolicy: None
